# Task 2: Session Tracker

This script reads a space-separated log file of user events (`IN` and `OUT`) and calculates the total minutes each user was active.

**Usage:**
`node process_logs.js sample_input.txt`

**Expected Output for `sample_input.txt`:**
Alice: 105 mins
Bob: 45 mins
Charlie: 30 mins

Currently, the script runs, but the output is wrong. Find the bug, fix it minimally, and document the root cause.