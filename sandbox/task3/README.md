# Task 3: Expense Splitter

This program reads a CSV of expenses (`Name,Amount`) and calculates the minimum number of transactions required to settle the debts fairly. 

**Usage:**
`go run split.go expenses.csv`