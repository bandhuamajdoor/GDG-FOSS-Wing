I had a merge conflict as both branches had changed the same line in profile.txt differently.I manually resolved the conflict by removing the conflict markers and editing the file to keep the changes i wanted and then staged the resolved file and completed the merge with a commit



If the conflict was not intentional,i would use git diff before merging to compare the changes in both branches and resolve them beforehand

